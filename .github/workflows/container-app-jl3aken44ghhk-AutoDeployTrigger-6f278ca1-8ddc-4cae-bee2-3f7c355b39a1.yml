name: Trigger auto deployment for container-app-jl3aken44ghhk

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-jl3aken44ghhk-AutoDeployTrigger-6f278ca1-8ddc-4cae-bee2-3f7c355b39a1.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPJL3AKEN44GHHK_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPJL3AKEN44GHHK_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPJL3AKEN44GHHK_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPJL3AKEN44GHHK_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPJL3AKEN44GHHK_REGISTRY_PASSWORD }}
          containerAppName: container-app-jl3aken44ghhk
          resourceGroup: appspacesdemobf58
          imageToBuild: default/container-app-jl3aken44ghhk:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


